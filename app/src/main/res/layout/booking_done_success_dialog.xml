<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="10dp"
    app:cardBackgroundColor="@color/combo_background_green">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

    <ImageView
        android:id="@+id/iv_done"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_done_50"
        android:padding="40dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        app:tint="@color/combo_text_green" />

    <TextView
        android:id="@+id/tv_done"
        android:layout_below="@+id/iv_done"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textStyle="bold"
        android:textColor="@color/combo_text_green"
        android:textSize="50dp"
        android:text="@string/booking_done"
        />

    </RelativeLayout>

</androidx.cardview.widget.CardView>